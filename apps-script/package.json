{
  "name": "linkulino-apps-script",
  "version": "1.0.0",
  "private": true,
  "license": "MIT",
  "description": "Linkulino backend — Google Apps Script web app bound to the expenses spreadsheet, synced with clasp.",
  "scripts": {
    "login": "clasp login",
    "pull": "clasp pull",
    "push": "clasp push -f",
    "deploy": "clasp deploy",
    "open": "clasp open",
    "test": "node --test"
  },
  "devDependencies": {
    "@google/clasp": "^2.5.0"
  }
}
